#include <iostream>
using namespace std;

int main() {
    double a,b,c,d;
    char ch;
    
    cin >> ch >> a >> ch >> b >> ch >> ch >> ch >> c >> ch >> d >> ch;
    cout << (a+b)*(c-d) << " ";
    
    cin >> ch >> a >> ch >> b >> ch >> ch >> ch >> c >> ch >> d >> ch;
    cout << (a+b)*(c-d) << " ";
    
    cin >> ch >> a >> ch >> b >> ch >> ch >> ch >> c >> ch >> d >> ch;
    cout << (a+b)*(c-d);
    
    
    return 0;
}
